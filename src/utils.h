#ifndef _UTILS_H_
#define _UTILS_H_

#include <tgmath.h>

#define MAX_UNSAFE(x, y) ((x) > (y) ? (x) : (y))
#define MIN_UNSAFE(x, y) ((x) < (y) ? (x) : (y))
#define MIN(x, y) fmin
#define MAX(x, y) fmax


#endif
